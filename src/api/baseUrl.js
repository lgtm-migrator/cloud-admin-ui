/**
 * 网关路由
 * @type {string}
 * 废弃
 */
export const gatewayRouter = '/v1/api/gateway/router'
/**
 * 用户登录
 * @type {string}
 */
export const login = '/v1/server/system/oauth2/user/login'

/**
 * 注销
 * @type {string}
 */
export const logout = '/v1/server/system/oauth2/user/logout'

/**
 * 用户详情
 * @type {string}
 */
export const userInfo = '/v1/server/system/oauth2/user/info'

/**
 * 用户服务
 * @type {string}
 */
export const userServer = '/v1/server/system/user'
/**
 * 路由
 */
export const routerServer = '/v1/server/system/router'

/**
 * 菜单
 * @type {string}
 */
export const menuServer = '/v1/server/system/menu'

/**
 * 权限
 * @type {string}
 */
export const permissionServer = '/v1/server/system/permission'
/**
 * 权限菜单
 * @type {string}
 */
export const permissionMenuServer = '/v1/server/system/permissionmenu'

/**
 * 角色
 * @type {string}
 */
export const roleServer = '/v1/server/system/role'
